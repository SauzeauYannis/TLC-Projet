/*
 * Ce fichier sert a montrer les differentes erreurs possibles.
 * Les erreurs sont commentes pour que le fichier sexecute.
 * Il suffit de decommenter pour voir les messages derreurs. 
 */

var a;

// 1. Variable deja declaree
//var a;

// 2. Variable n'existe pas
//b <- 5;

// 3. Affectation d'une variable d'iteration
boucle b 0 10;
	deplacer (b, b);
	//b <- 1;
fin boucle;

// 4. Erreur de syntaxe
//baisser; lever; baisser

